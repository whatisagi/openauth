export interface Theme {
  primary?: {
    dark: string;
    light: string;
  };
  background?: {
    dark: string;
    light: string;
  };
  logo?: {
    dark: string;
    light: string;
  };
  css?: string;
}
